# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Local backups of the dataset (safety net, not needed in the repo)
_backup/

# Local tooling (Claude Code preview server config) and verification scratch files
.claude/
_*.html
_*.js
_data.json

# Generated by run_pipeline.py and by the daily GitHub Action.
# The dashboard/charts are rebuilt fresh and deployed to Pages, so they are not
# committed — except the history CSVs, which must persist for the over-time charts.
outputs/*
!outputs/history.csv
!outputs/golden_history.csv
outputs_pretournament/
compare.html
